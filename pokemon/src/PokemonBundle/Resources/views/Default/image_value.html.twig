{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% block field %}
    {% if object.image != null %}
        <img class="list_image" src="{{ object.getImageUrl() }}">
    {% else %}
        <span>Отсутствует</span>
    {% endif %}

{% endblock %}